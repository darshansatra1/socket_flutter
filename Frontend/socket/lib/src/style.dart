import 'package:flutter/material.dart';

ThemeData darkTheme = ThemeData(
    scaffoldBackgroundColor: mainBlack,
    primaryColor: orange,
    brightness: Brightness.dark);

Color orange = Color(0xFFF5B041);
Color mainBlack = Color(0xFF111820);
